import Link from "next/link";
import {notFound,redirect} from "next/navigation";
import Brand from "@/components/Brand";
import RunHistoryMap from "@/components/RunHistoryMap";
import {createClient} from "@/lib/supabase/server";
function fmt(sec:number){const h=Math.floor(sec/3600),m=Math.floor((sec%3600)/60),s=Math.floor(sec%60);return h?`${h}:${String(m).padStart(2,"0")}:${String(s).padStart(2,"0")}`:`${String(m).padStart(2,"0")}:${String(s).padStart(2,"0")}`}
export default async function RunHistoryPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const sb=await createClient();const {data:{user}}=await sb.auth.getUser();if(!user)redirect("/login");const {data:run}=await sb.from("runart_live_runs").select("id,course_id,distance_km,elapsed_seconds,avg_pace_sec_per_km,started_at,finished_at,track_geojson,runart_courses(name,start_name)").eq("id",id).eq("user_id",user.id).maybeSingle();if(!run)notFound();const pace=run.avg_pace_sec_per_km?`${Math.floor(run.avg_pace_sec_per_km/60)}:${String(run.avg_pace_sec_per_km%60).padStart(2,"0")}`:"--:--";return <main className="wrap"><header className="top"><Brand/><div className="nav"><Link className="btn ghost" href="/my">← MY</Link><Link className="btn" href={`/courses/${run.course_id}`}>코스 상세</Link></div></header><section className="detailHero card discoveryDetail"><div><span className="eyebrow">GPS RUN</span><h1>{(run.runart_courses as any)?.name||"러닝 기록"}</h1><p className="muted">{new Date(run.finished_at).toLocaleString("ko-KR")} · {(run.runart_courses as any)?.start_name||"출발점"}</p></div><div className="score"><b>{Number(run.distance_km).toFixed(2)}</b><span>km</span></div></section><div className="stats section"><div className="stat"><b>{Number(run.distance_km).toFixed(2)}</b><span className="muted">km</span></div><div className="stat"><b>{fmt(Number(run.elapsed_seconds||0))}</b><span className="muted">시간</span></div><div className="stat"><b>{pace}</b><span className="muted">분/km</span></div><div className="stat"><b>{run.track_geojson?.coordinates?.length||0}</b><span className="muted">GPS 포인트</span></div></div><section className="section"><div className="sectionHead"><div><span className="eyebrow">MY TRACK</span><h2>내가 달린 GPS 궤적</h2></div></div>{run.track_geojson?.coordinates?.length?<RunHistoryMap track={run.track_geojson}/>:<div className="card muted">저장된 GPS 궤적이 없습니다.</div>}</section></main>}
